/* exported setValue */

function setValue(object, key, value) {
  object[key] = value;
  return object;
}

// input: {}
// output: {key: value}

// set object with the key in brackets equal to the value given
// return the object from the function
