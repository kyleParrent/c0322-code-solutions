/* exported getLastChar */

function getLastChar(string) {
  var last = string.length - 1;
  return string[last];
}

// input: 'hello'
// output: 'o'

// get the strings length and subract one to get the last index
// and asssign that to a varible
// return that varible created inside of the string's index
